# 技术改动清单

本文档记录已讨论的技术改动及其实施状态。TC-001～TC-005 已于 2026-08-03 完成实现和验证；TC-006 已完成授权范围内实现和验证，但真实新批次端到端性能门禁受共享缩略图路径阻断。TC-007 已完成共享图片服务、两端渐进交付、扫描、Android 联合并发和导出复制六条支线的实现与验证；两端预热门禁和网络字节目标通过，非预热冷路径仍有明确未达项。最终行为和确切限制已同步到现行产品 Spec。

## 改动索引

| 编号 | 标题 | 状态 |
| --- | --- | --- |
| TC-001 | 扩大推荐去重历史并修正“最近入库”重复 | 已实施并验证（2026-08-03） |
| TC-002 | 为图片推荐增加单张右键菜单 | 已实施并验证（2026-08-03） |
| TC-003 | 修正 Android 推荐查看与反馈交互 | 已实施并验证（2026-08-03） |
| TC-004 | 跨桌面与 Android 共享个性化推荐偏好 | 已实施并验证（2026-08-03） |
| TC-005 | 移除“最近入库”推荐来源 | 已实施并验证（2026-08-03） |
| TC-006 | 优化推荐候选、向量选择与偏好画像性能 | 已实施；端到端门禁受阻（2026-08-03） |
| TC-007 | 优化图片交付并审计共享 I/O 并发 | 已实施并验证；冷路径部分未达（2026-08-03） |

## TC-001：扩大推荐去重历史并修正“最近入库”重复

### 背景

推荐页每批返回 15 张图片。当前每个 viewer 只读取最近 60 张已成功展示图片作为排除历史，相当于仅覆盖 4 个完整批次；第 5 批开始，最早一批图片会重新获得推荐资格。

现有数据验证结果：

- 桌面 viewer 的 17 个批次共返回 255 张图片，其中只有 202 个唯一 SHA-256，产生 53 次重复推荐。
- 45 次重复恰好间隔 5 个批次发生，46 次属于 `recent` 槽位再次进入 `recent` 槽位。
- LAN viewer 同样出现以 5 个批次为主要间隔的重复。
- 同一批次内部不存在 SHA-256、文档 ID 或候选 ID 重复，`shown` 事件也已正常写入。

因此问题不是推荐事件丢失或数据库只保存 60 条记录，而是去重历史的读取上限过小，且 `recent` 槽位只按入库时间排序，历史窗口放开后会重新选择同一组最新图片。

### 目标与成功标准

- 每个 viewer 的强去重历史至少覆盖 200 张已成功展示的图片。
- 采用 240 张历史窗口，覆盖 16 个完整批次。
- 候选充足且每个批次均成功提交 `shown` 时，最近 16 个批次内不得出现相同 SHA-256。
- `recent` 槽位存在未展示候选时，不得优先返回已经展示过的图片。
- `random` 槽位存在未展示候选时，不得因随机种子排序而优先返回已经展示过的图片。
- 候选不足时允许逐级缩小排除窗口或返回部分批次，但同一批次内仍不得复制图片补足数量。
- 保持 viewer 隔离、请求幂等、曝光计数和现有推荐配额不变。

### 技术方案

1. 将推荐历史读取上限从 60 调整为 240。
2. 将历史排除窗口调整为 `240 → 210 → 180 → 150 → 120 → 90 → 60 → 30 → 0`，仅在候选不足以组成完整批次时逐级放宽。
3. `recent` 槽位每次选择严格限定在当前最低 viewer 展示次数层级，再按 `mtime_ns` 降序和稳定候选 ID 排序，避免后续多样性评分越级选回高曝光候选。
4. `random` 槽位每次选择严格限定在当前最低 viewer 展示次数层级，再使用现有请求种子执行稳定随机排序。
5. 将每个候选池的采样上限从 100 调整为 256，避免扩大历史窗口后候选池本身被完全排除。继续保持有界采样，不执行全库随机排序或全量加载。
6. 继续复用现有 `events` 和 `content_stats` 数据，不修改 SQLite 表结构，不迁移或清空既有推荐历史。

### 修改范围

实现时仅允许修改以下范围：

- `image_vector_service/recommendation_store.py`：历史读取上限。
- `image_vector_service/recommendations.py`：排除窗口以及 `recent`、`random` 排序规则。
- `image_vector_service/backend_server.py`：候选池采样上限。
- `tests/test_recommendation_store.py`、`tests/test_recommendations.py`、`tests/test_recommendation_backend.py` 和受采样参数直接影响的既有推荐测试。
- `docs/spec.md`：实现完成后同步最终行为。

不得修改前端、数据库 Schema、图片索引、搜索、智能标注、发布矩阵或其他无关模块；不得借本项改动顺手重构推荐代码。

### 验证项

- 单元测试：240 条历史可按最近成功展示顺序读取，viewer 之间互不影响。
- 算法测试：连续生成并标记至少 17 个批次，前 16 个批次不存在相同 SHA-256。
- 排序测试：`recent` 和 `random` 均优先选择未展示或低曝光候选。
- 降级测试：不足 15 个合格候选时逐级缩小窗口，最终仍不足则返回 `partial`，且批次内无重复。
- 幂等测试：相同 `request_id` 继续回放原批次，不新建不同队列。
- 冒烟测试：使用本地推荐数据库连续刷新推荐页，核对批次数量、`shown` 事件以及 SHA-256 重复间隔。

### 本项不包含

- 不合并桌面端与 Android/LAN viewer 的推荐历史；跨设备仍按现行设计隔离。
- 不在本项中增加跨批次相册或角色冷却。若仍需降低“不同文件但画面相似”的重复感，应另立改动项讨论相册配额、角色配额或向量相似历史。
- 不删除、重写或清空现有推荐记录。

## TC-002：为图片推荐增加单张右键菜单

### 背景

TC-002 实施前，Windows 图片推荐页只在卡片底部提供“打开、喜欢、导出、不喜欢”按钮，推荐图片本身没有右键菜单。搜索结果页已有右键操作，但其中“相关/不相关”表示图片与当前查询的相关性，并绑定搜索结果的多选状态，不能直接照搬为推荐偏好。

### 目标与成功标准

- 在 Windows 图片推荐页右键任意推荐图片时，打开只针对该图片的操作菜单。
- 菜单提供：打开详情、系统打开、所在文件夹、喜欢、不喜欢、复制图片、复制文件、复制文件路径、导出到文件夹。
- 推荐菜单使用“喜欢/不喜欢”及现有 `like/dislike` action；搜索结果菜单继续使用“相关/不相关”，不得改变搜索学习语义。
- “喜欢”和“不喜欢”互斥，并正确显示当前批次内该图片的最新偏好状态；操作失败时保持原状态并显示现有错误提示。
- 系统打开成功后继续记录 `open`，导出实际成功后继续记录 `export`；打开详情和复制操作不新增后端 action 类型。
- 菜单必须限制在可视窗口内，并支持点击菜单外、按 Escape、窗口缩放或切换页面时收起。
- 原生操作进行期间防止对同一图片重复提交。
- 推荐卡片仅显示图片与图片右下角的推荐来源角标；移除图片下方的文件名、图库名和“打开/喜欢/导出/不喜欢”按钮。单击图片继续打开，右键菜单、推荐详情及其中既有操作保持不变。

### 交互范围

- 右键只定位当前图片，不创建推荐页多选状态。
- 不显示“已选择 N 张”“支持 Ctrl / Shift 跨页保留选择”和“清除选择”。
- 不支持 Ctrl/Shift 多选或批量复制、批量导出。
- “复制文件”和“导出到文件夹”均只处理当前右键图片。
- 打开详情使用推荐结果已有的预览图、文件名、图库、推荐来源、标签、尺寸和文件大小；不得伪造搜索匹配度、搜索排名或查询相关性字段。

### 技术方案

1. 推荐卡片删除图片下方的信息和按钮区域，仅保留可单击、可右键的图片及图片右下角来源角标；捕获 `contextmenu`，阻止浏览器默认菜单，并保存当前 `RecommendationItem` 与经过窗口边界约束的菜单坐标。
2. 扩展现有图片右键菜单组件的单张模式和反馈文案配置：搜索场景保持现有结构，推荐场景隐藏多选提示、撤销搜索反馈和清除选择，并发出 `like/dislike` 事件。
3. 推荐页通过现有 `useRecommendations.recordAction` 提交 `like/dislike`，复用当前 pending、重试 event ID 和偏好状态逻辑，不新增推荐 API。
4. 通过 `App.vue` 将现有原生图片操作按单张回调传给推荐页，复用 `mediaId` 执行系统打开、定位文件、复制图片、复制文件、复制路径和导出。
5. 推荐详情使用 feature 内独立详情视图，仅读取 `RecommendationItem` 已有字段，避免把推荐结果伪装成 `SearchResultItem` 或改动搜索详情语义。

### 修改范围

实现时仅允许修改以下范围：

- `frontend/src/features/recommendations/RecommendationPage.vue`：右键状态、菜单接线和详情入口。
- `frontend/src/features/recommendations/RecommendationDetail.vue`：推荐图片详情视图；仅在实现时确有需要才新增。
- `frontend/src/components/GalleryContextMenu.vue`：增加不影响搜索默认行为的单张推荐模式和偏好文案配置。
- `frontend/src/App.vue`：向推荐页传递现有单张原生操作回调。
- `frontend/src/features/recommendations/__tests__/RecommendationPage.spec.ts`、推荐详情测试、右键菜单组件测试及受新增 props 直接影响的 `App` 测试。
- `docs/spec.md`：实现完成后同步最终交互契约。

不得修改推荐后端 action 集合、数据库 Schema、搜索反馈含义、Android 推荐页、推荐选择算法、发布矩阵或其他无关模块；不得借本项改动重构整个图库菜单或原生操作层。

### 验证项

- 组件测试：右键推荐图片打开菜单，菜单目标和坐标正确，浏览器默认菜单被阻止。
- 卡片测试：仅渲染图片和推荐来源角标，不渲染文件名、图库名或底部操作按钮；单击图片仍触发现有打开行为。
- 文案测试：推荐场景显示“喜欢/不喜欢”，搜索场景仍显示“相关/不相关”。
- 状态测试：喜欢和不喜欢互斥，pending 期间禁用重复操作，失败后不误改状态。
- 原生操作测试：系统打开、定位、三种复制和导出均只收到当前图片的 `mediaId`；仅成功打开和成功导出记录对应推荐 action。
- 收起测试：点击其他区域、Escape、窗口缩放和离开推荐页均关闭菜单。
- 详情测试：仅展示推荐结果已有字段，关闭、系统打开和定位文件可用。
- 视觉检查：在 light mode、dark mode 和窄窗口下截图检查菜单边界、对齐、文本对比度及详情布局。
- 冒烟测试：在 Windows WebView 推荐页完成一次右键喜欢、复制、定位、导出和关闭菜单流程。

### 本项不包含

- 不增加推荐图片多选或批量操作。
- 不把搜索结果页的“相关/不相关”全局改名。
- 不为 Android 增加桌面式右键交互。
- 不新增撤销推荐偏好的后端接口。

## TC-003：修正 Android 推荐查看与反馈交互

### 背景

Android 推荐页存在三个关联的交互问题：

- 推荐页进入全屏图片查看器后，系统返回或关闭查看器会回到“搜索”Tab，而不是来源“推荐”Tab。
- 推荐网格卡片直接显示“喜欢/不喜欢”，用户尚未查看原图细节即可反馈。
- 全屏查看器长按打开底部菜单后，点击“喜欢”或“不喜欢”只提交反馈，菜单不会收起。

返回页错误的直接原因是 `SearchScreen` 在内部使用 `remember` 保存 `selectedTab`，而全屏查看器通过 `ZvecApp` 的提前返回替换整个 `SearchScreen`。关闭查看器后 `SearchScreen` 被重新创建，Tab 默认值重新变为 `SEARCH`。搜索结果因另有搜索结果副作用会再次切换到结果页，推荐页没有对应恢复逻辑。

### 目标与成功标准

- 从 Android 推荐 Tab 打开任意图片后，通过系统返回手势、系统返回键或查看器关闭操作退出时，必须回到推荐 Tab。
- 返回推荐 Tab 后保留原推荐批次、滚动数据和已加载图片，不重新生成批次，也不跳转到搜索或结果 Tab。
- 推荐网格卡片不再显示“喜欢/不喜欢”或反馈状态，点击卡片只进入全屏查看器。
- 推荐反馈仅保留在全屏查看器的长按底部菜单中，使用户先查看图片再决定喜欢或不喜欢。
- 在长按菜单点击“喜欢”或“不喜欢”后立即收起菜单，再异步提交且只提交一次对应 action。
- 搜索结果查看器的返回行为、保存、分享、旋转和详细信息操作保持不变。

### 技术方案

1. 将 `MobileTab` 选择状态提升到不会因全屏查看器提前返回而离开组合的位置，即 `ZvecApp` 根级的 `rememberSaveable` 状态；`SearchScreen` 改为接收当前 Tab 和切换回调。
2. 将推荐网格 `LazyGridState` 同样提升到 `ZvecApp`，打开推荐查看器时保留当前 `RECOMMENDATIONS` Tab、原批次和滚动位置；`closeViewer` 仍只清理 viewer source、index 和传输状态，不通过推荐数据存在与否猜测返回页。
3. 从 `RecommendationCard` 删除 reaction、pending、`onLike`、`onDislike` 参数和按钮区域；`RecommendationPanel` 不再向卡片传递列表内反馈回调。
4. 全屏查看器继续只为 `RecommendationItem` 在长按菜单显示“喜欢/不喜欢”。两个按钮均先将 `sheetItem` 设为 `null` 关闭 `ModalBottomSheet`，随后调用现有 `onReaction`。
5. 继续复用 `AppViewModel.reactToRecommendation` 的 pending、幂等 event ID 和错误恢复逻辑，不新增 action、接口或持久化字段。

### 修改范围

实现时仅允许修改以下范围：

- `android/app/src/main/java/com/zvec/lanviewer/ui/ZvecApp.kt`：Tab 状态提升、推荐卡片按钮移除和长按菜单收起顺序。
- 与上述交互直接对应的 Android Compose UI/状态测试；若新增测试文件，仅放在 `android/app/src/androidTest/java/com/zvec/lanviewer/ui/` 或现有 Android UI 单元测试目录。
- `docs/spec.md`：实现完成后同步 Android 推荐交互。

原则上不需要修改 `AppViewModel.kt`、网络层或数据模型。不得修改 Windows 推荐页、推荐后端、数据库 Schema、推荐选择算法、搜索流程、发布矩阵或其他无关部分；不得为本项引入新的导航框架。

### 验证项

- 导航测试：推荐 Tab → 打开推荐图片 → 系统返回，断言仍显示推荐 Tab 和原批次。
- 导航回归：结果 Tab → 打开搜索结果 → 系统返回，仍显示结果 Tab。
- 列表测试：推荐卡片不包含“喜欢”“不喜欢”“已喜欢”或“已跳过”按钮，点击图片仍打开正确索引。
- 菜单测试：推荐原图长按显示喜欢/不喜欢；搜索结果原图长按不显示推荐反馈。
- 收起测试：点击喜欢或不喜欢后 `ModalBottomSheet` 立即消失，并只发送一次对应 action；网络请求尚未完成时也不得保持菜单展开。
- 回归测试：保存和分享仍先收起菜单；旋转和详细信息仍可在菜单内连续操作。
- 视觉检查：在 Android light mode、dark mode 和窄屏设备上截图，确认移除按钮后的卡片信息密度与对齐正常。
- 冒烟测试：在 Android 真机或模拟器上完成“推荐 → 查看原图 → 长按 → 喜欢 → 返回推荐”的完整流程。

### 本项不包含

- 本项不定义或修改 Windows 推荐卡片交互；Windows 最终行为以 TC-002 为准。
- 不取消推荐系统的 `like/dislike` action，只调整 Android 的反馈入口。
- 不在推荐网格卡片上新增长按菜单；长按菜单仍属于全屏原图查看器。
- 不改变点击推荐图片时现有的 `open` action 记录。

## TC-004：跨桌面与 Android 共享个性化推荐偏好

### 背景

桌面端和 Android 当前都会把推荐 `like/dislike` action 写入电脑端 `recommendations.sqlite3`，但事件按 viewer 标识记录，客户端只在当前内存状态中显示反馈结果，推荐选择算法也不读取这些偏好。用户已明确要求桌面和手机共享同一套偏好。

共享范围限定为显式偏好：所有连接到同一台 YaoLens 后端且已获授权的客户端共同使用一个默认偏好档案。批次、`shown` 历史和曝光次数继续按 viewer 隔离，不能因共享偏好而合并各设备的浏览历史。

### 目标与成功标准

- 桌面和所有已授权 Android 设备共享同一套图片喜欢/不喜欢状态。
- 同一 SHA-256 在不同设备产生冲突反馈时，以电脑数据库中最后成功写入的 `like/dislike` 事件为最终状态。
- 现有历史 `like/dislike` 事件直接参与共享偏好，不要求用户重新反馈，也不得清空或重写原事件。
- 已明确喜欢或不喜欢的原图不再向任何设备重复推荐；喜欢图片仅用于提升相似且未展示图片，不喜欢图片用于降低相似图片排序。
- 个性化只读取明确的 `like/dislike`。`shown` 仅用于曝光与历史去重，`open` 只表示查看，`export` 继续作为操作事件但不在本项中推断为喜欢。
- 至少累计 10 张具有最终显式偏好的不同图片后启用个性化；强度随有效反馈增加并设置上限，不允许少量反馈立即主导整批结果。
- 保持技术质量、最近入库、低曝光和随机发现的固定配额；随机发现继续承担探索职责，不按偏好向量改造成同质化推荐。
- 个性化不得临时调用模型，不保存图片路径或向量到推荐数据库，只复用各图库已经存在的图片向量。
- 客户端重启或更换设备后，服务端返回的最终偏好仍可恢复“已喜欢/已不喜欢”状态。

### 偏好与排序规则

1. 共享档案固定属于当前电脑端 YaoLens 实例，不新增用户账号。事件仍保留原 `viewer_id` 作为操作来源，但计算最终偏好时跨 viewer 合并。
2. 对每个 SHA-256，只取 sequence 最大的 `like/dislike` 事件作为当前偏好；历史重复点击不会增加该图片在画像中的权重。
3. 生成推荐前先排除所有已有最终显式偏好的原图，再应用 TC-001 的 viewer 展示历史排除，避免喜欢过的图片因个性化加分而反复出现。
4. 从有可用现有向量的喜欢图片生成正向偏好画像，从不喜欢图片生成负向偏好画像；已删除、向量缺失或向量空间不兼容的反馈仍保留最终状态，但不参与向量画像。
5. 个性化仅在 `quality`、`recent` 和 `low_exposure` 候选内部提供有上限的排序加减分，再继续执行现有相册/角色配额和向量多样性惩罚。`random` 保持稳定随机排序。
6. 有效反馈不足 10 张、没有可用偏好向量或跨图库向量空间不兼容时，回退到现行非个性化排序，并在响应中给出不含隐私数据的原因。
7. 个性化强度固定为 `min(0.25, effective_count × 0.005)`：10 条有效向量反馈时为 5%，50 条起达到 25% 上限。喜欢与不喜欢分别生成单位向量均值画像，候选 affinity 为可用正向相似度与负向相似度取反后的均值，最终调整严格限制在 `[-0.25, +0.25]`，并由确定性测试固定。

### 数据与接口方案

- 继续以现有 `events` 和 `items` 为事实来源，通过 `item_id` 关联 SHA-256、library ID 和 doc ID，并按事件 sequence 计算每张图片的最终共享偏好。
- 不新增偏好表，不改变现有四表 Schema。实测 `EXPLAIN QUERY PLAN` 后增加精确候选偏好查询使用的 `idx_items_sha256`、`idx_events_item_action_sequence`，以及有界画像事件倒序读取使用的部分覆盖索引 `idx_events_preference_sequence`；未被 planner 使用的候选索引不保留。
- 推荐画像按 sequence 倒序最多扫描最近 4096 条 `like/dislike` 事件，在该有界窗口内按 SHA-256 首次出现取最终状态并最多保留 256 张，再按图库 worker 读取现有向量，不能跨线程直接访问 Collection。禁止新增偏好表意味着无法同时保证固定读取工作量与任意重复历史下始终凑满全历史最新 256 个不同 SHA；极端重复反馈只让较旧图片退出画像并安全降级，不影响全历史最终偏好、原图排除或批次回放的精确性。
- 推荐响应为每个当前批次项增加可选最终偏好字段，并返回个性化是否应用、有效反馈数和安全原因码。降级码为 `insufficient_preferences`、`vectors_unavailable`、`incompatible_vector_spaces`；幂等批次回放使用 `replayed` 且仍读取 item 当前最终偏好。action 成功响应同样返回与事件写入原子读取的当前最终偏好，避免较旧 event ID 在跨设备冲突后回放时覆盖客户端显示。不得返回其他设备 ID 或完整反馈历史。
- Windows 与 Android 均以服务端偏好为准恢复显示状态。手机不另建一套独立偏好数据库，成功提交到电脑后即对所有设备生效。
- 相同 `event_id` 继续保持幂等；网络失败不得提前在客户端永久显示成功状态。

### 修改范围

实施核验发现，backend 已返回 action 幂等结果，但 Windows 与 LAN 两层 facade
会丢弃该结果；若不贯通 action 最终偏好，旧 event ID 在跨设备冲突后回放时
客户端会显示错误状态。因此在不改变路由、action 集合或授权边界的前提下，
将范围最小扩展到现有 action 响应转发链路：

- `image_vector_service/recommendation_store.py`：跨 viewer 最终偏好查询及必要索引。
- `image_vector_service/recommendations.py`：有界个性化评分、冷启动回退和显式偏好原图排除。
- `image_vector_service/backend_server.py`：加载共享偏好、按图库读取偏好向量并扩展推荐响应。
- `image_vector_service/service.py`：仅增加按已知 doc ID 有界读取现有向量所需的图库 worker 接口；不得重新生成嵌入。
- `zvec_host/recommendation_service.py`：安全转发偏好与个性化响应字段。
- `zvec_webview/server.py`、`zvec_webview/lan_access.py`、`zvec_lan/http_server.py`、`zvec_lan/models.py`：仅贯通并过滤 action 响应中的 `recorded/preference`，不改变路由或鉴权。
- `frontend/src/features/recommendations/`：解析并显示服务端最终偏好与个性化状态。
- `android/app/src/main/java/com/zvec/lanviewer/data/model/Models.kt`、`data/network/LanApiClient.kt`、`data/repository/ZvecRepository.kt`、`AppViewModel.kt` 和推荐相关 UI：解析 action 最终偏好并恢复共享状态；网络层只把现有 Unit 响应改为安全解析返回值。
- 对应 Python、Windows 前端和 Android 推荐测试。
- `docs/spec.md`、`docs/android-lan-api-v1.md`：实现完成后同步共享偏好及响应契约。

不得修改图片嵌入模型、重建 Collection、合并 viewer 的 `shown`/曝光历史、上传偏好或图片到新的外部服务、改变发布矩阵，或修改搜索学习及其他无关模块。

### 验证项

- 共享状态测试：桌面喜欢一张图片后，Android 读取同一最终偏好；Android 随后不喜欢时，桌面读取到最新不喜欢状态。
- 历史兼容测试：升级前已经写入的桌面和 LAN `like/dislike` 事件无需迁移即可参与最终偏好计算。
- 去重测试：任何设备已明确反馈的 SHA-256 不再进入新批次；已有批次回放仍能显示最新共享偏好。
- 隔离回归：桌面与各 Android viewer 的批次、`shown` 历史和曝光计数仍彼此独立。
- 冷启动测试：少于 10 个不同显式偏好时选择结果与非个性化基线一致。
- 排序测试：喜欢相似候选获得有上限的提升，不喜欢相似候选受到降权；基础配额、历史排除、相册/角色限制和 MMR 仍生效。
- 探索测试：`random` 槽位不受偏好画像排序，只排除已经明确反馈或被历史窗口排除的原图。
- 向量兼容测试：缺失、删除或不兼容向量只使对应反馈退出画像计算，不丢失偏好状态，也不触发模型请求。
- 幂等与失败测试：相同 event ID 不重复写入；提交失败时其他设备不得看到未落库状态。
- 性能测试：偏好加载和向量读取保持有界，在大图库中不扫描完整 Collection、不执行全库向量查询。
- 冒烟测试：桌面喜欢 → Android 换一批并查看状态 → Android 不喜欢相似图片 → 桌面换一批，验证共享状态与排序方向。

### 本项不包含

- 不合并不同设备的展示历史或曝光计数。
- 不增加多用户账号、云同步或跨电脑共享。
- 不把打开图片自动解释为喜欢。
- 不在手机离线时建立独立偏好队列；本项以电脑端成功落库为共享生效条件。
- 不自动删除原始反馈事件。

## TC-005：移除“最近入库”推荐来源

### 背景

TC-001 将 viewer 去重历史扩大到 240 张，并让原 `recent` 槽位优先选择低曝光候选，但该槽位仍会持续围绕入库时间最新的一小组图片排序；历史窗口在候选不足时放宽后，这组图片仍容易再次出现。用户已明确不需要“最近入库”作为独立推荐来源，因此本项移除算法槽位，而不是只隐藏前端角标。

### 目标与成功标准

- 新生成的推荐批次不再包含 `bucket=recent`，界面不再出现“最近入库”文案。
- 每批仍保持 15 张，固定配额调整为技术质量 5、低曝光 6、随机发现 4。
- 移除按 `mtime_ns` 倒序读取的最近入库候选采样，避免继续为已取消来源加载候选。
- `low_exposure` 和 `random` 在存在未展示候选时，不得因个性化或向量多样性评分越级选回已展示候选。
- 升级前已经持久化的 `recent` 批次仍可幂等回放；旧项不得被过滤或导致解析失败，Windows 显示通用“推荐”，Android 继续安全读取。
- 不清空推荐数据库，不迁移或重写旧批次、shown 历史和反馈事件。

### 技术方案

1. 从活动推荐槽位与固定配额中删除 `recent`，将其 4 张配额分配为 `low_exposure +2`、`random +2`，总数保持 15。
2. 候选采样只保留技术质量池和稳定随机池；低曝光排序继续基于这两个有界候选池的并集及 viewer 曝光计数，不增加全库扫描。
3. 将最低曝光层级的硬约束从原 `recent` 转移到 `low_exposure`，并保留 `random` 的同类约束；个性化只作用于 `quality` 与 `low_exposure`。
4. 推荐数据库中的 `slot` 继续按字符串保存且不做 Schema 变更。批次回放链路继续接受旧 `recent`，但新选择器永远不生成该值。
5. Windows 客户端保留对旧 `recent` wire value 的解析兼容，来源角标和详情统一显示为“推荐”；Android 的字符串 bucket 与带默认值的 quota 模型保持兼容，新契约不再返回活动 `recent` 配额。

### 修改范围

本项仅允许修改以下位置：

- `image_vector_service/recommendations.py`：活动槽位、配额及低曝光选择约束。
- `image_vector_service/state.py`：删除最近入库候选查询。
- `frontend/src/features/recommendations/RecommendationPage.vue`、`RecommendationDetail.vue`、`useRecommendations.ts`：移除用户可见文案并保留旧批次兼容。
- 与上述行为直接对应的 Python、Windows 和 Android 推荐测试。
- `docs/spec.md`、`docs/android-lan-api-v1.md`、`docs/technical-changes.md` 与推荐说明文案。

不得修改 `recommendation_store` Schema、240 张去重历史、共享偏好语义、搜索流程、图片索引模型、发布矩阵或其他无关部分；再次强调，不得借本项修改无关页面、格式或邻近代码。

### 验证项

- 算法测试：完整批次配额为 5/6/4，所有新项均不含 `recent`。
- 候选测试：每图库候选上限从三池并集降为两池并集，仍同时覆盖技术质量与稳定随机候选。
- 排序测试：`low_exposure` 和 `random` 均不会在未展示候选存在时被 MMR 越级到更高曝光层级。
- 回放测试：数据库中已有 `recent` 项可经后端和 Windows 客户端读取，并只显示通用“推荐”。
- Android 契约测试：缺少 `recent` quota 字段时使用兼容默认值并正确读取 5/6/4 新配额。
- 冒烟测试：连续生成批次，确认响应、Windows 推荐页和 Android 推荐页均不出现“最近入库”。

### 本项不包含

- 不删除旧批次或旧事件。
- 不改变批次大小、240 张历史窗口、相册/角色限制或显式偏好排除。
- 不在本项中实施 MMR 向量归一化缓存、偏好画像缓存或其他独立性能优化。

## TC-006：优化推荐候选、向量选择与偏好画像性能

### 背景与基线

TC-005 后的纯选择基线使用 `tools/benchmark_recommendations.py`、固定种子 `20260803`、候选构造不计时、预热 3 次并正式运行 10 次。当前机器实测如下：

- 300 候选 × 1024 维：中位数 1412.339 ms，P95 1481.323 ms。
- 768 候选 × 1024 维：中位数 3818.171 ms，P95 3846.726 ms。

现行实现为每个图库固定读取每类 256 个候选；每选择一个槽位都会重新执行基础排序，并让每个剩余候选与全部已选项重新计算余弦和向量范数；每次换批也会重新读取最多 256 个偏好向量并重建画像。这些重复工作不改变结果，却直接放大候选规模和向量维度带来的延迟。

### 目标与成功标准

- 正常请求每个图库先按每类 100 个候选读取；只有首轮无法在完整 240 历史下返回 15 张，或固定 5/6/4 配额发生降级时，才扩到每类 256 并完整重选一次。
- 保留 `240 → 210 → 180 → 150 → 120 → 90 → 60 → 30 → 0` 的历史放宽顺序，不以缩短历史换取速度；首轮和扩容中间结果均不得提前落库。
- 每个选择调用只校验并归一化一次候选向量、只建立一次各槽位稳定基础排序；选中图片后增量维护其他候选相对已选集合的最大相似度惩罚，保持现有 MMR 阈值和选择语义。
- 缓存已归一化正向/负向偏好画像、有效反馈数、向量空间摘要和最后显式偏好 sequence；后端重启首次重建，连续换批复用。
- `shown/open/export`、同状态重复反馈和幂等事件回放不得使画像重建；只有某 SHA-256 的最终 `like/dislike` 状态实际变化时，下一批才失效重建。
- 分别记录候选读取（含首轮/扩容）、偏好画像、选择排序、结果回填和总耗时，并记录候选读取次数与画像缓存命中；字段只含有界数值或布尔值，不含路径、viewer/device ID、请求标识、图库标识或偏好内容。
- 当前机器性能门禁：768×1024 纯选择 P95 不超过 1 秒；真实图库冷启动首批不超过 3 秒；热缓存端到端“换一批”P95 不超过 2 秒。

### 技术方案

1. 候选读取先以 `limit_per_pool=100` 执行；使用同一 240 条历史、显式偏好排除和固定请求 seed 完成渐进选择。若结果不是完整 15 张、历史窗口已放宽，或任一活动槽位少于固定配额，则以 256 重新读取、重新加载新增候选的曝光和显式偏好，并只持久化最终选择。
2. 纯选择入口在候选稳定去重后，一次生成 `quality/low_exposure/random` 排序、基础分和合法单位向量。每个历史窗口使用独立的已选状态与最大惩罚映射；每选中一张，只以其单位向量遍历候选并更新最大惩罚。
3. 将偏好向量预处理拆为“构建不可变画像”和“将画像应用到当前候选”两步。推荐单线程 worker 持有一份电脑级缓存；反馈事务返回最终偏好、显式 sequence 和最终状态是否变化，供 worker 精确失效缓存。
4. 后端推荐响应增加安全 `performance` 对象；Windows 本地 bridge 只按字段白名单透传，LAN/Android 响应保持现有契约。后端阶段耗时不含 HTTP 和缩略图预载；端到端实测另从点击换批到缩略图预载、shown 同步及按钮恢复完成为止计时。
5. 微基准固定候选生成、seed、配额、结果摘要、预热和采样次数；正确性测试同时锁定确定性、渐进历史、5/6/4 配额、最低曝光层、random 不个性化、无效/缺失/不兼容向量、共享偏好和旧 `recent` 回放。

### 修改范围

本项仅允许修改以下位置：

- `image_vector_service/recommendations.py`：画像预处理与等价的增量多样性选择。
- `image_vector_service/recommendation_store.py`：在现有事务内返回安全的偏好变化和 sequence；不修改 Schema。
- `image_vector_service/backend_server.py`：100/256 候选读取编排、画像缓存及安全阶段耗时。
- `zvec_host/recommendation_service.py`：仅白名单透传 Windows 本地阶段指标，LAN 调用不透传。
- `tools/benchmark_recommendations.py` 及直接对应的 Python 推荐测试。
- `docs/spec.md`、`docs/technical-changes.md`：同步最终性能行为和实测结果。

不得修改 `state.py` 候选查询语义、推荐数据库四表、240 条历史、viewer 隔离、共享偏好事实规则、模型调用策略、搜索、前端或 Android 业务代码、发布矩阵及其他无关模块；再次强调，不得借性能改动改变 5/6/4 选择结果语义或取消向量多样性。

### 验证项

- 候选读取：正常为 `[100]`；240 历史不足或配额降级时严格为 `[100, 256]`；扩容仍不足时不继续循环，不落中间批次；幂等回放不重新读取。
- 选择等价：固定输入下优化前后 item ID、slot、顺序、history window 和配额一致；基础排序每槽一次、每个候选向量归一化一次，并显著减少相似度计算。
- 降级回归：缺失、零向量、NaN/Inf、维度不兼容、相册/角色限制、SHA 去重和 MMR `.85/.95` 阈值均保持确定性。
- 缓存：冷批读取一次偏好向量；热批、shown/open/export、同状态反馈和幂等回放均复用；最终偏好实际变化后下一批恰好重建一次，重启后的新 manager 首批重建。
- 安全指标：阶段字段只接受已列出的非负数值、计数和布尔值，恶意路径、viewer、图库、请求或偏好字段均不得经过 bridge。
- 性能：同一微基准重跑 300/768 两档并通过 768 P95 门禁；真实图库记录冷启动、至少 20 次热换批 P50/P95、首轮命中/扩容次数及各阶段汇总。

### 实测结果与确切限制（2026-08-03）

同一脚本、固定 seed `20260803`、1024 维、候选构造不计时、预热 3 次并正式运行 10 次的纯选择结果如下；优化前后选择摘要完全一致：

| 候选数 | 优化前 Median / P95 | 优化后 Median / P95 | Median 减少 | P95 减少 |
| ---: | ---: | ---: | ---: | ---: |
| 300 | 1412.339 / 1481.323 ms | 228.698 / 234.419 ms | 1183.641 ms（83.81%，6.18×） | 1246.904 ms（84.18%，6.32×） |
| 768 | 3818.171 / 3846.726 ms | 580.587 / 615.905 ms | 3237.584 ms（84.79%，6.58×） | 3230.821 ms（83.99%，6.25×） |

- 300 候选摘要：`6021a28914d429a769226f5f679ba3beb23ea7d8685a4888cdb0a4ecabb71ecf`。
- 768 候选摘要：`c0811e391efd727b86c8c580549cc08060abdf5e9cafdf21c524531ba3bb4de7`。
- 768×1024 纯选择 P95 为 615.905 ms，通过 1 秒门禁。

真实图库 A/B 使用当前推荐数据库的两个隔离副本、相同 viewer、相同请求序列和各自空缩略图缓存；未修改原始用户数据库。测量包含 gateway create HTTP、推荐数据库、候选/画像/选择/回填、15 张缩略图 HTTP 预载和 shown HTTP，不包含 DOM commit/paint：

- `df4961e` 基线：冷启动首批 7924.960 ms；20 次热进程新批次 P50/P95 为 8041.641 / 8734.548 ms。
- 优化后：冷启动首批 2662.453 ms，通过 3 秒门禁；首轮 20 次新批次 P50/P95 为 2539.851 / 3209.072 ms。
- 进一步预热数百张缩略图后，再取 20 个新的真实批次，端到端 P50/P95 仍为 2510.269 / 2995.028 ms；create HTTP 为 564.133 / 611.148 ms，其中后端 `total` 为 495.763 / 536.604 ms，缩略图 HTTP 预载为 1931.967 / 2371.225 ms，shown HTTP 为 24.751 / 41.470 ms。
- 该 20 批均完整返回 15 张，20/20 使用首轮每池 100、0 次扩容、20/20 命中画像缓存。300 次缩略图请求仅约 8% 命中既有缓存；240 条历史和随机探索使真实新批次大多仍需首次生成缩略图。
- 完全相同 request/event ID 的幂等重放 P50/P95 为 122.341 / 141.275 ms，但该路径不读取候选、不执行选择且不是真实“换一批”，只作为全热缩略图诊断，不计作门禁通过。

因此，纯选择和冷启动首批门禁已通过，真实新批次端到端 P95 `≤ 2 秒` 尚未通过。瓶颈已经定位到 `zvec_webview/image_registry.py` 和前端共同使用的缩略图生成/HTTP 预载路径，而 TC-006 的授权范围明确排除共享图片服务和前端。修改渲染并发或预载策略可能同时影响搜索与图库，不能在本项中越界处理；本项不得标记为全部性能门禁通过。

### 本项不包含

- 不缩短或取消 240 条历史，不合并 viewer 曝光。
- 不保存画像、路径或原始向量到推荐数据库，不新增偏好表。
- 不调用新模型、不取消向量多样性、不改变 random 探索语义。
- 不改变 LAN、Windows 或 Android 的推荐功能交互和发布矩阵。

## TC-007：优化图片交付并审计共享 I/O 并发

### 背景与未优化基线

TC-006 已把真实新批次的推荐后端 P95 降到约 0.537 秒，但当时包含 15 张缩略图 HTTP 预载的 Windows 端到端 P95 仍为 2.995 秒，其中首次缩略图生成/传输约占 2.371 秒。共享 `ImageRegistry` 的生产渲染上限为 2；Windows 同时发起 15 个预载后等待全部完成才提交，Android 则把推荐 `thumbnail_url` 指向 `/original`，下载 15 张原图并等待全部 Coil 任务成功。Android 推荐 capability 创建还会逐项读取完整原图计算 SHA-256。

在 `4a6f88d` 的只读图库快照、独立推荐数据库和空缩略图缓存上复测，生产渲染并发 2 的冷首批前 6 张/全 15 张分别为 1674.8/2212.0 ms；20 个全新批次的前 6 张（含 create）P50/P95 为 1493.6/1718.5 ms，全 15 张 P50/P95 为 1864.1/2183.8 ms，现行等待全批后的可见 P50/P95 为 1873.8/2188.4 ms。

Android/LAN 基线另由 immutable、clean 的 `4a6f88d` 重跑，生产 render=2、共享 HTTP Dispatcher=10；该版本的 create 会为每批 15 项逐一读取原图 SHA，列表 URL 也全部指向 `/original`。20 个全新批次 create P50/P95 为 736.068/816.774 ms，click-to-fixed-t4 为 849.919/1004.296 ms，click-to-fixed-t6 为 854.777/1004.357 ms，click-to-all-t15 为 905.306/1264.731 ms；shown/open/status P95 分别为 29.052/31.097/14.722 ms。300 张列表原图共传输 1,634,786,151 bytes，每批 P50/P95 为 77,606,521.5/125,378,959 bytes；20 批各 15 张、300 项无重复、所有请求零错误。两组基线均使用全新 request/event ID，不以幂等回放冒充换批。

本项同时审计会争用相同 CPU、磁盘和 HTTP 服务的图片扫描与大批量导出复制；高并发档只用于找拐点和淘汰不公平组合，不代表生产值。

### 目标与门禁

- 共享缩略图固定测试 2/3/4/6/8/12/16；生产只采用主要收益拐点处的最小有界值。任一搜索缩略图、preview 或轻量请求 P95 相对 2 回退超过 10%，或 CPU、内存、I/O 峰值明显恶化的档位不得采用。
- Windows 全 15 张同时预载，固定前 6 张成功即把含 15 个槽位的批次提交为可见；非预热前 6 张 P95 不超过 1.5 秒，全 15 张不超过 2 秒，预热命中点击提交 P95 不超过 0.6 秒，冷首批前 6 张不超过 2 秒。
- Android 推荐使用鉴权的 640px `/thumbnail`，固定前 4 张成功即提交；非预热前 4 张 P95 不超过 1.5 秒，全 15 张不超过 2.2 秒，预热命中点击提交 P95 不超过 0.6 秒，列表网络字节比旧 `/original` 至少减少 80%。详情、保存和分享继续使用原图。
- 图片扫描以 4/6/8/12 测试同一只读输入；大批量导出复制以 1/2/4 测试同一只读源和隔离临时目标。只有吞吐稳定改善、结果/摘要/顺序/取消语义相同且搜索、推荐、缩略图和轻量响应回退均不超过 10%时，才提高生产值。
- Android 客户端 HTTP/图片请求以 10/15/16 做单变量筛选，再与服务端缩略图低档、拐点和高档组成有代表性的联合矩阵。shown/action 或轻量 API 回退超过 10%、出现饥饿、错误、超时或资源峰值明显恶化的组合直接淘汰。
- Windows 与 Android 同时使用共享服务时不得出现明显不公平；不以降低图片尺寸、JPEG 质量、preview 画质或缓存容量换速度。

### 六条实施与验证支线

#### A. 共享缩略图渲染并发

可重复基准对每档使用 fresh 子进程、相同有序真实图片和独立临时缓存，至少执行 20 组 × 15 张冷缩略图，并保留全热与争用对照。每档记录固定前 6 张和全 15 张 P50/P95、错误、进程 CPU 均值/100ms 采样峰值、工作集峰值、Windows `GetProcessIoCounters` 增量及临时磁盘缓存精确字节。进程 I/O 包含文件和 loopback socket，不等同于物理磁盘吞吐；该局限必须随结果保留。生产仍复用 `BoundedSemaphore`、同图 single-flight、内存/磁盘缓存和原子写入，不建立无界线程池。

正式矩阵使用 340 张同序真实只读图片，source/version 顺序摘要为 `f82dc960…40dc8`；七档均为 20×15、fresh 子进程和独立临时缓存。冷、全热、争用、HTTP request、decode、render 错误全部为 0：

| render workers | 冷前 6 P50 / P95 | 冷全 15 P50 / P95 | 全热前 6 P50 / P95 | 全热全 15 P50 / P95 |
| ---: | ---: | ---: | ---: | ---: |
| 2 | 878.547 / 1943.186 ms | 953.775 / 2043.746 ms | 526.568 / 1027.335 ms | 527.336 / 1033.316 ms |
| 3 | 832.924 / 1503.068 ms | 860.176 / 1503.068 ms | 522.845 / 1029.439 ms | 525.425 / 1029.439 ms |
| 4 | 739.741 / 1167.010 ms | 763.692 / 1391.105 ms | 520.191 / 527.921 ms | 524.307 / 1024.739 ms |
| 6 | 739.141 / 1132.441 ms | 759.045 / 1207.867 ms | 524.946 / 1030.530 ms | 526.648 / 1030.530 ms |
| 8 | 654.237 / 1131.069 ms | 1092.437 / 1288.640 ms | 523.291 / 1007.877 ms | 527.730 / 1028.185 ms |
| 12 | 667.946 / 1127.739 ms | 683.680 / 1129.472 ms | 524.788 / 529.494 ms | 525.326 / 1031.231 ms |
| 16 | 660.957 / 1283.420 ms | 677.617 / 1283.420 ms | 521.517 / 1033.002 ms | 525.772 / 1033.002 ms |

| render workers | 争用前 6 P95 | 争用全 15 P95 | 搜索缩略图 P95 | preview P95 | bootstrap P95 | CPU 平均 / 峰值 | 峰值 RSS |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 2 | 2359.681 ms | 2359.681 ms | 1217.024 ms | 1356.490 ms | 521.062 ms | 148.179% / 377.857% | 396.344 MiB |
| 3 | 1524.349 ms | 1768.834 ms | 1352.707 ms | 1469.896 ms | 518.321 ms | 174.536% / 445.972% | 499.137 MiB |
| 4 | 1309.751 ms | 1442.548 ms | 940.176 ms | 1063.011 ms | 520.355 ms | 200.692% / 521.742% | 627.535 MiB |
| 6 | 1246.358 ms | 1302.976 ms | 942.156 ms | 1096.444 ms | 522.516 ms | 208.698% / 717.596% | 883.941 MiB |
| 8 | 1233.399 ms | 1240.899 ms | 1005.674 ms | 1137.170 ms | 521.846 ms | 218.153% / 898.101% | 1121.773 MiB |
| 12 | 1133.720 ms | 1139.175 ms | 1013.446 ms | 1195.669 ms | 1017.546 ms | 241.768% / 1081.992% | 1378.840 MiB |
| 16 | 1162.512 ms | 1162.512 ms | 1058.820 ms | 1190.482 ms | 521.088 ms | 239.497% / 1214.200% | 1528.934 MiB |

每档都执行 61,544 次读操作、620 次写操作，进程逻辑读/写分别为 3,888,825,533 / 24,968,615 bytes，临时缓存均为 620 项 / 24,968,615 bytes；输入和逻辑 I/O 工作量一致。`GetProcessIoCounters` 同时包含受 Windows 文件缓存影响的文件和 loopback I/O，不能解释为物理盘吞吐。15 路原生 HTTP 对照还会受到本地监听队列影响，因此全热绝对值只作共享请求诊断，真实浏览器/Android 端到端另行测量。

生产选择 4：相对 2，冷前 6 P95 减少 39.943%，冷全 15 P95 减少 31.934%，且搜索缩略图、preview 和 bootstrap 均无超过 10% 的 P95 回退；3 的冷前 6 为 1503.068 ms，略未通过 1.5 秒门禁。6 相对 4 的冷前 6 P95 只再改善 2.962%，但峰值 CPU 增加 37.54%、峰值 RSS 增加 40.86%；8/12/16 收益继续递减，12 的 bootstrap P95 回退 95.55%，16 的冷前 6 P95 反而比 4 高 9.97%。4 的压力峰值 RSS 仍比 2 增加 231.191 MiB（58.33%），这是必须由真实源码版联合冒烟继续验证的明确资源代价，不能描述为“内存无增长”。

正式证据为 `thumbnail-matrix-7.json`（SHA-256 `D3A130145A7DF1CB17283C1B52D2093EFAEE7D41D22A7C7DBC2321B3E6F2F446`）；不可变前值另保存在 `paired-baseline-4a6f88d.json`（`F1E1F61460806C116CAACD450CF84D616E079DB1C932FC66016F6F56222B7C06`）。

#### B. Windows 渐进展示与单批预热

新批次同时启动所有缩略图预载，只等待前 6 张；关键组失败保留旧批次且不写 shown，尾部失败不回滚并只汇总一次。批次只在推荐页和 document 均可见时提交，提交后即按“15 个图片槽位已经可见”记一次 shown。当前批 shown 成功且尾部全部 settle 后，内存里最多静默准备一个下一批；预热批未消费时不得 shown。点击可消费完整或在途预热而不重复 create/preload。页面隐藏、卸载或最终显式偏好实际变化会精确取消/失效预热；open/export、最终状态相同的反馈及幂等回放不失效。

#### C. LAN 真实缩略图与 Android 渐进展示/预热

新增鉴权的 `GET/HEAD /api/v1/media/{media_id}/thumbnail`，沿用 session owner capability、撤销和安全 404，返回路径无关的 bytes/content type/ETag，支持 private 缓存、`If-None-Match`/304 和安全大小上限。推荐响应只把 `thumbnail_url` 指向该路由，`preview_url` 保持 `/original`。推荐 capability 创建只绑定 registry ID 和文件版本；强 SHA-256 延迟到首次 original 请求并按稳定版本 single-flight 缓存。

Android 同时启动 15 个 Coil `size(640)` 预载，固定前 4 张成功即提交，尾 11 张后台 settle；关键失败不提交，尾错不回滚。Tab RESUMED、shown 成功且尾部 settle 后最多预热一个下一批；点击接管、偏好精确失效、切 Tab/STOP/断开/重新配对/clear 取消及 generation 防迟到均与 Windows 语义对应。未消费预热不计曝光。

#### D. 图片扫描并发审计

现行扫描使用有界 `ThreadPoolExecutor`，生产默认 4，in-flight 默认是 worker 的两倍；发现结果带 sequence，并在 worker 完成后按 sequence 回填。4/6/8/12 基准对同一真实只读图片顺序分别使用隔离 staging/运行目录，记录吞吐、批次与单项 P50/P95、错误、取消延迟与残留、CPU/工作集/进程 I/O，以及争用期间搜索、推荐、缩略图和轻量响应。不得改变发现顺序、SHA 去重、错误事实、进度、取消或恢复语义。

正式矩阵从当前唯一启用图库按稳定顺序选取 340 张、1,846,689,211 bytes 的真实源内容，一次性复制为同一 D: 卷的 owned 隔离快照；复制耗时 1031.141 ms，不计入扫描档位。源卷、snapshot 和 staging 均为当前实际 `CT2000MX500SSD1` SATA SSD。快照构造与内容摘要会预热 Windows 文件缓存，三轮轮换顺序只能减少后跑档位偏差，结果不是严格冷盘结论，也不得泛化到 HDD 或其他 SSD。

| scan workers | 总耗时 P50 / P95 | 吞吐 P50 | 单项 inspect P50 / worst P95 | CPU 平均中位 / 峰值 | 峰值 RSS | 错误 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 4 | 1836.720 / 1861.424 ms | 185.113 images/s | 10.043 / 89.083 ms | 236.081% / 402.682% | 102.418 MiB | 0 |
| 6 | 1744.642 / 1773.009 ms | 194.882 images/s | 14.034 / 97.126 ms | 245.668% / 557.329% | 109.605 MiB | 0 |
| 8 | 1724.964 / 1741.256 ms | 197.106 images/s | 19.049 / 88.850 ms | 272.465% / 572.203% | 124.203 MiB | 0 |
| 12 | 1745.304 / 1834.702 ms | 194.808 images/s | 61.091 / 87.646 ms | 310.720% / 568.747% | 138.918 MiB | 0 |

| scan workers | 搜索缩略图 worst P95 | preview worst P95 | bootstrap worst P95 | 纯推荐选择 worst P95 | 逻辑读 / 写中位 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 4 | 52.365 ms | 49.189 ms | 48.437 ms | 40.082 ms | 1,891,257,803 / 676,212 bytes |
| 6 | 58.739 ms | 59.092 ms | 51.102 ms | 41.649 ms | 1,891,257,803 / 676,212 bytes |
| 8 | 64.518 ms | 66.134 ms | 54.003 ms | 43.730 ms | 1,891,257,803 / 676,212 bytes |
| 12 | 80.741 ms | 80.165 ms | 62.470 ms | 44.921 ms | 1,891,257,803 / 676,212 bytes |

四档三轮的扫描计数、失败事实、sequence/path/SHA 摘要全部与 4 相同，源与 snapshot 前后摘要不变，staging 和 scanner thread 无最终残留。4→6/8 的吞吐只提高 5.28%/6.48%；6 已使搜索缩略图和 preview 的 worst P95 相对 4 回退 12.17%/20.13%，超过 10% 淘汰线，12 的吞吐还回落且 CPU/RSS 继续上升。因此生产扫描并发保持 4，不修改 `ServiceConfig`。

正式证据为 `scan-matrix-4.json`（SHA-256 `94CB0251B0548A5515E295DD980803A0AFB7D31EE8E88E13AF70772BBDCB0728`）。

真实中途取消在 4/6/8/12 下分别于 777.961/671.157/640.661/641.049 ms 返回，均观察到取消、无线程或 staging 残留。final-drain 场景在请求取消前 340 项已经完成，因此 scanner 自身未再观察取消，26.239–37.705 ms 后返回一个 ready staging；现行 service 紧接着的 `cancel_check` 才会终止任务，显式 cleanup 后残留为 0。不得把这一既有边界写成 scanner 在 final drain 内响应了取消。

#### E. Android HTTP 与服务端渲染联合并发

Android 现行 `LanApiClient` 使用同一个有界 OkHttp Dispatcher，并把 `maxRequests` 与 `maxRequestsPerHost` 都设为 10；Coil 复用该 client。先单变量测试 10/15/16，再把保留档与服务端 2/3/4/6/8/12/16 中有代表性的低档、拐点和高档组合，避免无意义的 21 组全笛卡尔积。每个组合使用真实 LAN、全新批次、独立服务端缩略图缓存和受控 Coil 缓存，记录两端资源、网络字节、前 6/全 15、预热命中、shown/action/轻量 API 公平性、错误、超时和取消。生产组合必须是通过公平性门禁的最小有效值。

正式代表矩阵由 10x2、10x4、15x4、16x4、10x6、10x12、16x12 七组组成；每组在 API 34 模拟器与真实 LAN 上执行 20 个 foreground 和 20 个 prepared 全新 request，服务端使用独立推荐库和空缩略图缓存。每组 600 个正式缩略图二次 Coil 读取全部命中 memory/disk cache，prepared 消费前 shown=0、消费后 shown=20；网络、解码、render、超时和业务错误均为 0，取消探针延迟 20.304–119.913 ms，迟到完成和 OkHttp Dispatcher residual 均为 0。表中 t4 是从 create 开始到 Android 固定前 4 张完成；prepared click 只含生产 preloader 状态物化，不含 Compose paint：

| Android HTTP x render workers | create P50 / P95 | foreground t4 P50 / P95 | foreground t6 P50 / P95 | foreground t15 P50 / P95 | prepared click P50 / P95 | shown / action / status P95 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 10x2 | 538.708 / 1393.878 ms | 1935.826 / 2630.113 ms | 2339.045 / 2791.553 ms | 2673.262 / 3922.102 ms | 0.052 / 0.080 ms | 247.328 / 52.295 / 1061.131 ms |
| 10x4 | 542.085 / 1340.183 ms | 1534.545 / 2574.374 ms | 1646.417 / 2574.374 ms | 1953.302 / 3016.169 ms | 0.052 / 0.083 ms | 99.640 / 55.077 / 685.522 ms |
| 15x4 | 537.059 / 1159.532 ms | 1821.773 / 2878.587 ms | 1836.642 / 2929.440 ms | 1964.214 / 2987.909 ms | 0.051 / 0.122 ms | 58.007 / 66.879 / 560.124 ms |
| 16x4 | 539.481 / 787.398 ms | 1786.769 / 2728.457 ms | 1790.950 / 2776.880 ms | 1905.706 / 2776.880 ms | 0.054 / 0.167 ms | 47.512 / 52.169 / 572.127 ms |
| 10x6 | 536.603 / 1340.119 ms | 1470.431 / 2683.426 ms | 1506.109 / 2683.426 ms | 1638.648 / 2865.057 ms | 0.052 / 0.136 ms | 78.553 / 71.359 / 689.566 ms |
| 10x12 | 553.515 / 939.589 ms | 1494.838 / 2054.477 ms | 1526.126 / 2182.703 ms | 1631.073 / 2257.920 ms | 0.051 / 0.080 ms | 61.425 / 62.464 / 704.504 ms |
| 16x12 | 558.628 / 1230.901 ms | 1611.224 / 2621.697 ms | 1644.678 / 2621.697 ms | 1681.957 / 2621.697 ms | 0.054 / 0.125 ms | 62.031 / 44.114 / 622.330 ms |

| Android HTTP x render workers | Android CPU avg / peak / PSS peak | Windows CPU avg / peak / RSS peak | 热搜索缩略图 / preview / 轻量 HTTP P95 | 缩略图 Content-Length 合计 |
| ---: | ---: | ---: | ---: | ---: |
| 10x2 | 32.395% / 181.132% / 145.890 MiB | 203.619% / 850.217% / 577.195 MiB | 20.391 / 18.808 / 24.143 ms | 27,601,611 bytes |
| 10x4 | 49.969% / 212.195% / 140.329 MiB | 290.422% / 599.531% / 928.793 MiB | 20.497 / 18.907 / 24.291 ms | 27,601,611 bytes |
| 15x4 | 51.178% / 213.592% / 150.792 MiB | 288.477% / 645.403% / 924.066 MiB | 20.364 / 18.753 / 24.377 ms | 27,817,415 bytes |
| 16x4 | 51.621% / 209.009% / 147.681 MiB | 289.358% / 655.453% / 908.652 MiB | 20.626 / 18.804 / 24.532 ms | 27,601,611 bytes |
| 10x6 | 59.121% / 216.666% / 148.910 MiB | 341.087% / 790.066% / 1183.738 MiB | 20.518 / 18.884 / 24.654 ms | 27,601,611 bytes |
| 10x12 | 61.156% / 208.571% / 152.588 MiB | 361.911% / 1129.056% / 1339.949 MiB | 20.420 / 19.343 / 25.343 ms | 27,601,611 bytes |
| 16x12 | 62.890% / 235.514% / 149.592 MiB | 360.196% / 1301.842% / 1612.027 MiB | 21.672 / 19.738 / 28.027 ms | 27,601,611 bytes |

生产保持 Android HTTP=10、共享 render=4。15x4 相对 10x4 的 t4 P95 回退 11.82%、action P95 回退 21.43%，已被单变量淘汰；16x4 的 t4 P95 也回退 5.98%，其 t15 改善不足以证明更高客户端队列有主要收益。10x6 的 action P95 回退 29.56%且 CPU/RSS 上升；10x12 虽缩短 t4/t15，却让 action P95 回退 13.41%、Windows 峰值 RSS 增至 1339.949 MiB；16x12 还使热轻量 HTTP P95 相对 10x4 回退 15.38%。因此不补无意义的 15x低/高组合，也不提高 Android Dispatcher 或共享 render 上限。

接近空闲生产口径另以 10x4、20 foreground + 20 prepared、单个低频 Windows control 复测：create P50/P95 534.072/856.960 ms，foreground t4 1538.272/2427.430 ms，t15 1904.217/2601.598 ms，prepared click 0.051/0.087 ms；600 张正式缩略图为 27,601,611 bytes，零错误。相对 immutable `4a6f88d` 的 300 张原图 1,634,786,151 bytes，即使保守地用当前 600 张缩略图对比旧 300 张原图也减少 98.31%。预热点击门禁通过，但空缓存非预热 t4 `<=1.5 s` 和 t15 `<=2.2 s` 未通过；剩余瓶颈是首次真实大图 decode/640px render 与模拟器 Coil 解码，不能把背景预热命中冒充非预热达标，也不通过提高到已被资源/公平性淘汰的并发来掩盖。

正式证据为 `android-matrix-10x2.json`（SHA-256 `3374EF1643FD9942E9E52FE76354ACB8233B0B4AB2D172281B1ADE3BCD33CD8B`）、`android-matrix-remaining6.json`（`EC6D526D1E2457EB8F37C9DE06773C6E0E760C342D8A8C707890C85D099BB990`）和 `android-production-10x4.json`（`93AC62E2647BFF422524DE9D723608C080FE10319C15306E40BA96848F8CC114`）。工具输出不包含 token、origin、路径、设备 ID 或 request/event ID。

#### F. 大批量导出/复制并发审计

现行 Windows `NativeBridge._export_worker` 在输入顺序内串行执行 capability 解析、确定性目标命名、重名避让、`copy2`、失败记账和进度更新；`copy_files` 只向 Windows 剪贴板写文件路径列表，不执行磁盘复制，不属于本基准。1/2/4 基准使用同一真实只读源、相同目标顺序和每档独立显式临时目录，运行前后核对源摘要、输出摘要、完成/未完成清单、错误、取消口径、CPU/工作集/进程 I/O及争用请求。现有导出只有“取消文件夹选择即不创建任务”的取消边界，没有在途取消 API；本项不得擅自新增该产品功能。只有能保持命名、顺序、错误清单、进度、部分完成和现行取消契约且当前实际源/目标盘稳定获益时才并发化；否则生产保持 1。

正式矩阵使用同序 300 张、1,738,218,432 bytes 的真实只读源，每档和每轮使用独立 D: 临时输出目录；源与目标均为当前实际 `CT2000MX500SSD1` SATA SSD。1 路运行真实 production `NativeBridge` worker，2/4 路仅为工具内严格有界候选；每档均精确读取和写入 1,738,218,432 逻辑 bytes。三轮所有正常输出、重复 ID、不同目录同名、目标预置冲突、缺失源、partial 状态、错误 JSON、进度摘要均与 1 路 oracle 相同，源前后摘要不变，零非预期错误和线程残留。

| copy workers | 总耗时 P50 / P95 | 吞吐 P50 | 单项 copy P50 / worst P95 | CPU 平均中位 / 峰值 | 峰值 RSS |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 2042.911 / 2709.187 ms | 146.849 files/s | 2.535 / 21.164 ms | 84.886% / 176.625% | 119.801 MiB |
| 2 | 1268.560 / 1314.405 ms | 236.489 files/s | 2.920 / 30.690 ms | 128.284% / 199.678% | 120.121 MiB |
| 4 | 1686.492 / 1994.461 ms | 177.884 files/s | 19.169 / 51.440 ms | 100.984% / 285.057% | 120.375 MiB |

| copy workers | 搜索缩略图 worst P95 | preview worst P95 | bootstrap worst P95 | 纯推荐选择 worst P95 | 预期外错误 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 45.915 ms | 46.191 ms | 43.582 ms | 41.125 ms | 0 |
| 2 | 52.120 ms | 52.898 ms | 49.735 ms | 46.709 ms | 0 |
| 4 | 53.205 ms | 53.902 ms | 48.074 ms | 44.694 ms | 0 |

2 路相对 1 路中位吞吐提高 61.04%，但搜索缩略图、preview、bootstrap 和纯推荐选择 worst P95 分别回退 13.51%、14.52%、14.12%、13.58%，全部超过 10% 淘汰线；4 路吞吐低于 2 路且交互回退仍超限。因此 production export worker 保持串行 1，不修改 `NativeBridge`。文件夹选择取消 worst 为 0.021 ms、零 job/零输出；运行中导出没有 cancel API，结果明确记为 `unsupported`，不得用 benchmark-only 行为冒充产品能力。该同盘 SATA SSD 结论不泛化到跨盘、NVMe 或 HDD。

正式证据为 `export-matrix-3.json`（SHA-256 `0E5A5694EA65DE59E4729BCA4E3AA362EA991716D8E0C4C4050027C6887B0E49`）。

### 最终端到端结果与门禁

Windows 源码版在同一真实图库上的冷首批前 6 张 DOM 提交/全 15 张就绪为 1.510/1.850 秒。20 个非预热、全新 request 的 create P50/P95 为 574.250/601.126 ms，前 6 为 1503.959/1751.808 ms，全 15 为 1862.943/2130.634 ms，shown 为 18.130/39.687 ms；20 个完整预热命中的点击提交 P50/P95 为 310.5/317 ms，全就绪为 330/341 ms，fallback=0、缩略图错误=0。相对 TC-006 历史端到端 P95 2.995028 秒，前 6 原始口径减少 41.51%（1.71x），加 shown 的保守口径减少 40.18%（1.67x），全 15 减少 28.86%（1.41x），预热点击减少 89.42%（9.45x）。冷首批前 6 `<=2 s` 与预热点击 `<=0.6 s` 通过；非预热前 6 `<=1.5 s` 和全 15 `<=2 s` 未通过。正式结果为 `windows-delivery-current-v2.json`（SHA-256 `B38D76AA1F06F44EFF6227251AC0AB68CB5A000DE8060D2DE80AF5279DA4EF32`）与 `windows-browser-prepared-20.json`（`56EF4BC78081F409064DDB1B88686C0D73DE7269E1F8DA65246D925D4CBEE8C6`）。

Android 近空闲 production 10x4 的 20 个非预热新批次 create P50/P95 为 534.072/856.960 ms，前 4 为 1538.272/2427.430 ms，全 15 为 1904.217/2601.598 ms；20 个预热命中的生产 preloader 状态提交 P50/P95 为 0.051/0.087 ms，不包含 Compose paint。600 张正式缩略图共 27,601,611 bytes；即使保守地与旧版 300 张原图 1,634,786,151 bytes 比较仍减少 98.31%，零网络、解码、render 和业务错误，取消 residual=0。预热点击与至少降低 80% 网络字节门禁通过；非预热前 4 `<=1.5 s`、全 15 `<=2.2 s` 未通过。一次未缓存详情原图实际接收 8,031,840 bytes；粗粒度检测为 5.552 秒，但含 1.5 秒静默确认和多次 `dumpsys`，只能视为上界，不能当作纯网络耗时。详情仍使用 `/original`，HEAD、Range 和强 ETag 语义不变。

联合真实源码冒烟中，bootstrap/job history P95 为 33.024/47.797 ms，相对隔离前值回退约 3.97%/4.04%，均低于 10%；搜索缩略图为 37.972 ms，preview 为 185.994 ms。没有新增图片错误，Android 280 个全新矩阵 request 也无错误；因此生产最终保持 render=4、Android HTTP=10、scan=4、export copy=1，不采用资源或公平性已淘汰的更高档。

### 回归、视觉与确切限制

- Python 推荐/LAN/WebView 最终定向回归为 207/207 passed；Ruff check/format、4 个相关文件的 Mypy 均通过。
- 前端 36 个文件、268/268 测试通过，typecheck 与 production build 通过；`frontend_dist` 仅为构建机械更新。
- Android JVM 62/62，通过 lint、`assembleDebug`、`assembleDebugAndroidTest`；API 34 connected 测试 13 项通过、1 个需显式 opt-in 的真实 LAN benchmark 跳过，其中 `RecommendationUiTest` 10/10 通过。
- Windows wide light/dark 已检查 15 张渐进网格、无遮罩、对比度以及右键菜单 Escape、点击外部收起；Android 正式 320dp light/dark、实际推荐页、长按反馈菜单、返回键收起菜单，以及从全屏返回推荐 Tab 并保留滚动位置均通过。
- 最终截图保存在非版本控制的 owned 临时证据目录：Windows light/dark/menu/Escape closed/outside closed 的 SHA-256 分别为 `8497D62446763E748A360932F888B7A00B7F875EE868B12C35B482E4932AACD0`、`76AB66726767C3BAAAAAA21BBB665D599B088FD31F42C73D7364FF7EDC013149`、`F4098645DFED0F7EBE97A542451392F39647DF0CEE7ACEEB55E726D38D34F4EF`、`4CE3109570AFC837C22DFD28BB1BE2075FF57C8118235D383558873E29D3090C`、`D7AA749EAA3015AD3878F45DD6E24BF778D23B1592938B74403B143008BB548C`；Android 320dp light/dark/fullscreen menu/menu back closed/return scroll preserved 分别为 `CBC4A9A78A72C145A4D94C2F5940328EC26536346FCA5B575139747BE30F37E9`、`069A9A07AEE487C4505B53C53E63EBD72733640E884470E79E56F95423DE3AFC`、`9320C9C61880BD35F73AC7DD713BB4D05AC8FF2D9D7CC3BBDB302A2AF17AFBB0`、`1A5BB4A11DC5EEB781716A50AACD00529B33FE49D9F471E842FBC331DC2EBE68`、`A7724E299B0305ECCE97005E7A24663614A3D434476022A3D84AD82F96B04AA8`。Android 返回前后的可见 item 文本与 bounds 完全一致；系统 night mode 与模拟器尺寸在验收后均已恢复。
- Windows 浏览器 viewport 在设置 720/480 时仍固定为 1280x720，真实 WebView 又有 `min_size=(980,680)`；因此不能声明 720px/480px 窄窗口视觉检查通过。产品受支持最小窗口内的宽屏结果有效，窄窗口是工具与产品最小尺寸共同造成的明确未覆盖项。
- 外部正式基准 JSON 曾按上述 SHA-256 核验；临时缓存、推荐数据库、扫描 staging、任务状态和导出目标均使用 owned 隔离目录且未污染用户数据。结果受当前 SATA SSD、Windows 文件缓存、真实图库内容、API 34 模拟器和同机 loopback/LAN 环境约束，不泛化为所有磁盘、手机或网络。

### 修改范围与禁止项

本项只允许修改共享 `ImageRegistry` 生产并发接线、Windows 推荐 composable、LAN thumbnail 路由/adapter、Android 推荐与现有 OkHttp/Coil 直接接线、扫描直接边界和 Windows `NativeBridge` 导出 worker（后二者必须先有基准证据）、对应测试、可复跑基准工具、机械前端 production build，以及本文件、`docs/spec.md`、`docs/android-lan-api-v1.md`。不得修改搜索报告 `result_exporter`、推荐算法、5/6/4 配额、240 历史、共享偏好或 viewer 曝光事实、搜索语义、数据库 Schema、模型调用、640px/JPEG/preview 画质、缓存容量、配对认证或发布矩阵；不得污染用户图库、索引、推荐数据库、缩略图缓存、任务记录或正式导出目录。
