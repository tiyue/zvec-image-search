# Windows x64 / CPython 3.12 only.  The build script rejects every other host.
# Reuse the locked core runtime and PyInstaller toolchain.
-r requirements-packaging.txt

# pywebview 6.2.1 Windows dependency closure.  Keep every version exact so a
# Preview rebuild cannot silently switch its native/.NET bridge implementation.
bottle==0.13.4
cffi==2.1.0
clr_loader==0.3.1
proxy_tools==0.1.0
pycparser==3.0
pythonnet==3.1.0
pywebview==6.2.1
typing_extensions==4.16.0
watchdog==6.0.0
