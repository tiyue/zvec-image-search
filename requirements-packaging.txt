# Install this environment explicitly before building.  The build script never
# downloads packages or creates an environment on the user's machine.
-r requirements-lock.txt
PyInstaller==6.16.0
altgraph==0.17.5
packaging==26.2
pefile==2023.2.7
pyinstaller-hooks-contrib==2026.6
pywin32-ctypes==0.2.3
setuptools==83.0.0
